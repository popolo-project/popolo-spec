%table
  %thead
    %tr
      - headers.each do |header|
        %th= header
  %tbody
    - namespaces.each do |prefix,namespace|
      %tr
        %td
          %code= prefix
        %td
          %a(href=namespace)= namespace
